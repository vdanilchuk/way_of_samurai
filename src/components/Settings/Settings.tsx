import React from "react";
import s from './Settings.module.css';

type SettingsType = {
    title?: string;
}

export const Settings =(props: SettingsType) => {
    return (
        <div>Settings</div>
    )

}
