import React from "react";
import s from './Dialogs.module.css';

type DialosgsType = {
    title?: string;
}

export const Dialogs =(props: DialosgsType) => {
    return (
        <div>Dialogs</div>
    )

}
